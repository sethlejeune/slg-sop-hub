# Copy this file to .env (kept out of git) for local live generation,
# or set the same variable in Vercel → Settings → Environment Variables.
#
# Get a key at https://console.anthropic.com → API Keys
ANTHROPIC_API_KEY=
